-- Schemă D1 completă (oglindă a migrațiilor). Vezi `migrations/` pentru rulare.
-- Sursa unică de adevăr pentru toate datele business Avyron.

PRAGMA foreign_keys = ON;

-- Vezi migrations/0001_init.sql pentru CREATE-urile efective.
-- Acest fișier este referință pentru dezvoltatori (nu se aplică direct).
