# AVYRON handoff for Claude

Read [AGENTS.md](AGENTS.md) for repository and release rules, then [the mobile, marketing, account and backup runbook](docs/AVYRON_MOBILE_MARKETING_BACKUP.md) before changing those modules.

Keep the existing GitHub remote, Cloudflare resources, routes and authentication contract intact. Work on a task branch and preserve unrelated changes. Do not invoke Lovable, activate paid services, apply remote migrations, merge or deploy without task-specific authorization.

Marketing drafts require Cost Guard and exact-version approval before explicit publication. Never retry an uncertain external publish automatically. Account secrets stay encrypted on the server. Device sessions are operator-assisted, not an installed native remote-control agent. Backup baselines are permanently protected. The PWA caches only its public offline resources.

Local verification is not evidence that real Meta credentials, a production backup, or installation on a physical iPhone/Android device has been validated. Follow the runbook activation and recovery steps and report those boundaries honestly.

For `avyron.eu` and demo project connections, read [the domain handoff](docs/DEMO_DOMAINS.md). Ten explicit proxied slots are live; account for the shared Worker wildcard before assigning a project. Preserve the apex redirect and existing verification records.
