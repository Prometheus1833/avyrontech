# Smart Surveys rollout — 22 September 2026

## Scope and source

The user authorized the Surveys implementation, Cloudflare changes, internal OS integration and a maximum Smart Surveys AI budget of 25 RON/month. The configurator was excluded. Work is isolated in `codex/smart-surveys-2026-09-22`; [PR #11](https://github.com/Prometheus1833/avyrontech/pull/11) remains open, without merging `main` or publishing unrelated worktree changes.

The release includes the existing OS centers and Documents Hub prerequisites (`72677b9`, `55df3b1`). The separate mobile/marketing/backup task must integrate and renumber its unpublished migration after 0029 before deployment.

## Infrastructure and finance

- `surveys.avyron.ro` is an active custom domain on the existing `avyrontech` Worker. Existing domain routes were retained.
- D1 migrations 0025–0029 were applied; private storage reuses `avyron-files`.
- The existing Turnstile widget includes the new hostname. Live browser inspection confirms that its script and challenge iframe load. A successful real-user CAPTCHA completion is not claimed from the headless browser.
- Native `SURVEY_EMAIL` is restricted to `avyrontech@gmail.com`; no external sending provider or marketing connector was invented.
- `survey-brief` is active with RON limits: 2500 bani/month, 250 bani/day and 20 bani/request. A 2,000,000 application-token ceiling is a separate safety limit, not a statement of the account's Cloudflare free allowance.
- AI cost reservations use maximum input/output estimates, ECB conversion and a 50% buffer. Pricing review expires on 22 October 2026; execution fails closed until reviewed again.

## Validation

- App/Worker type checks and lint passed.
- Unit/integration suite: 287 tests passed, including real SQLite schema execution, survey authorization and durable AI retry cases.
- Local D1 audit: all 29 migrations, foreign keys and integrity passed.
- Static/Pages, standalone Worker and API dry-run builds passed.
- Browser suite: 69 tests passed; final rebuilt Surveys suite: 4 passed.
- Real production desktop/mobile inspection: no horizontal overflow or page JavaScript errors; phone, WhatsApp and email links present.
- Existing public website and app auth return HTTP 200; unauthenticated survey administration returns 401.
- Synthetic live survey: private noindex/no-store route, authenticated file upload/download, rejected anonymous file access, file removal, autosave/resume and idempotent submission all passed. No lead/client/project was created for the technical test.

## Operational findings resolved during rollout

D1's global `PRAGMA quick_check` returned `SQLITE_NOMEM`. The release script now permits a bounded fallback only for that specific provider failure. All 148 application tables, including FTS shadow tables, passed individual checks; foreign keys and FTS runtime checks also passed. Provider-owned metadata is excluded explicitly. Any actual integrity failure still stops release.

The first live model run exceeded the HTTP background execution lifetime and left delivery waiting. Email and AI now have independent outbox events. Automatic AI generation runs in the existing 15-minute scheduled handler, with leased jobs, interrupted-run recovery and at most three separately reserved attempts. The structured brief is available immediately. See [Cloudflare Worker duration limits](https://developers.cloudflare.com/workers/platform/limits/#duration).

## Recovery and handoff

The final application release uses commit `dc7bf7f`, deployed as Worker version `45a5f4cb-0b46-4b8e-8cb5-55e87dc859e3`. Git merge simulation against current main `f86b4c9` passed without conflicts.

The initial Surveys Worker version was `8d958c85-8970-43dd-9696-99c76746b227`; the previous production version was `07929a49-bbc9-4640-99d6-0689e26b2d65`. The initial pre-migration D1 bookmark was `00000459-00000000-000050ed-b7a9f8a22432f8f7b27cd2048c6ca101`. Restore a Worker version only after checking compatibility; do not delete additive migrations or restore the entire database over newer business data.

Cloudflare accepted the single result email for the synthetic survey and recorded `sent` without errors. The transactional outbox is `published`; Gmail inbox receipt has not been inspected. Invalid Turnstile submission returns HTTP 403.

The first model (`llama-3.1-8b-instruct-fp8`) rejected strict JSON Schema with provider error 5025. Additive migration 0029 preserves version 1 history and activates version 2 on `llama-3.3-70b-instruct-fp8-fast`. The estimator uses its verified rates of $0.293/M input and $2.253/M output; the RON policy is unchanged. See [model pricing](https://developers.cloudflare.com/workers-ai/models/llama-3.3-70b-instruct-fp8-fast/) and [JSON-mode support](https://developers.cloudflare.com/workers-ai/features/json-mode/).

A real reformulation test exposed unsupported extra service details. A vocabulary/inflection and negation gate now rejects such proposals without changing the original answer. Human confirmation remains required.

Final model verification succeeded using the production D1/KV/Workers AI bindings through a temporary localhost-only verifier of the same runtime function. The new draft was persisted in D1, source citations passed validation and the 3-bani cost reservation was recorded by Cost Guard. No approval was performed automatically. The scheduled handler had already been observed claiming its durable job; the final model correction was verified directly without waiting for another scheduled slot.

The final production `assist` endpoint returned HTTP 200 with an available proposal for the user's construction example in approximately 2 seconds. The guard rejected the earlier unsupported expansion; no proposal was automatically saved as a client answer.

Both synthetic records were archived and their links revoked after testing. Their one-day test retention remains in place; cost reservations and delivery history were preserved. The temporary local verifier was stopped. No testing endpoint was published.

The remaining Claude/UI handoff concerns visual polish, richer option illustrations and transitions described in README.md. No heavy WebGL dependency, external marketing connector or malware scanning service is claimed.
