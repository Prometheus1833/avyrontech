import { useEffect,useState } from 'react';
import { ArrowRight,ArrowUpRight,Check,Globe,Layers,ShieldCheck,Sparkles } from 'lucide-react';
import SurveyShell,{ContactLinks} from '@/components/surveys/SurveyShell';
import Turnstile from '@/components/site/Turnstile';
import { seedTemplates } from '@/shared/surveys/templates';
import { surveyRequest,surveyMessage } from '@/lib/surveysApi';
import { setPageMeta } from '@/lib/seo';
export default function SurveyLanding(){
 const [selected,setSelected]=useState('website'),[token,setToken]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[reset,setReset]=useState(0),[templates,setTemplates]=useState(seedTemplates.map(({id,template})=>({id,...template}))),[ready,setReady]=useState(false);
 const campaign=new URLSearchParams(window.location.search).get('campaign')||undefined;
 useEffect(()=>{setPageMeta({title:'AVYRON Smart Survey — Un început clar pentru proiectul tău',description:'Un brief inteligent, simplu și personal. Spune-ne despre afacerea ta, obiective și materiale. AVYRON Development pune ideile în ordine.',path:'/surveys'});const canonical=document.querySelector('link[rel=canonical]');if(canonical)canonical.setAttribute('href','https://surveys.avyron.ro/');document.querySelector('meta[property="og:url"]')?.setAttribute('content','https://surveys.avyron.ro/');surveyRequest<{data:{id:string;title:string;service:string;description:string}[];campaign?:string}>(`templates${campaign?'?campaign='+encodeURIComponent(campaign):''}`).then(r=>{setTemplates(r.data as typeof templates);setSelected(r.campaign||r.data[0]?.id||'');setReady(r.data.length>0);}).catch(e=>setError(surveyMessage(e)));},[campaign]);
 const start=async()=>{setBusy(true);setError('');try{const storageKey=`avyron-survey-start:${campaign||selected}`;let requestKey=sessionStorage.getItem(storageKey);if(!requestKey){requestKey=crypto.randomUUID();sessionStorage.setItem(storageKey,requestKey);}const attribution=Object.fromEntries([...new URLSearchParams(window.location.search)].filter(([k])=>k.startsWith('utm_')));const result=await surveyRequest<{token:string}>('start',{template:selected,campaign,turnstileToken:token,requestKey,attribution});sessionStorage.removeItem(storageKey);window.location.assign(`/s/${result.token}`);}catch(e){setError(surveyMessage(e));setReset(x=>x+1);setToken('');}finally{setBusy(false);}};
 const chosen=templates.find(t=>t.id===selected);
 return <SurveyShell><main><section className="survey-hero"><div className="survey-hero-copy"><p className="survey-eyebrow"><span className="survey-live-dot"/> UN ÎNCEPUT BUN SCHIMBĂ TOTUL</p><h1>Ideile tale.<br/><span>Un început clar.</span></h1><p className="survey-lead">Tu îți cunoști afacerea. Noi te ajutăm să transformi ceea ce ai în minte într-un proiect bine definit.</p><p className="survey-hero-note">Un interviu scurt, adaptat răspunsurilor tale. Fără cont, fără presiune. Îl poți continua în ritmul tău.</p><a className="survey-button primary" href="#alege">Să conturăm proiectul <ArrowRight size={19}/></a><div className="survey-trust"><span><Check size={15}/>Se adaptează la tine</span><span><ShieldCheck size={15}/>Răspunsuri private</span></div></div><div className="survey-orbit" aria-hidden="true"><div className="orbit-ring r1"/><div className="orbit-ring r2"/><div className="orbit-ring r3"/><div className="orbit-core">A<span>AVYRON</span></div><div className="orbit-chip chip1"><span>01</span> Ideile tale</div><div className="orbit-chip chip2"><Sparkles size={16}/> Mai multă claritate</div><div className="orbit-chip chip3"><Check size={16}/> Un brief pentru echipă</div><span className="orbit-caption">DE LA CONVERSAȚIE LA DIRECȚIE</span></div></section><section className="survey-choice-section" id="alege"><div className="survey-section-heading"><div><p className="survey-eyebrow">ALEGE PUNCTUL DE PORNIRE</p><h2>Ce construim împreună?</h2></div><p>Începem cu ce contează.<br/>Detaliile apar pe parcurs.</p></div><div className="survey-selection"><div className="survey-service-grid">{templates.map(({id,...template},i)=><button type="button" key={id} className={`survey-service ${id===selected?'selected':''} ${id==='website'?'featured':''}`} onClick={()=>setSelected(id)} disabled={!!campaign} aria-pressed={id===selected}><span className="service-icon">{i===0?<Globe size={23}/>:<Layers size={20}/>}</span><span>{id==='website'&&<small>PRIMUL PAS RECOMANDAT</small>}<strong>{template.service}</strong>{id==='website'&&<em>Un website care spune cine ești și aduce oamenii potriviți mai aproape.</em>}</span><span className="service-check">{id===selected?<Check size={16}/>:<ArrowUpRight size={16}/>}</span></button>)}</div><aside className="survey-start-card"><span className="survey-eyebrow">BRIEF PERSONALIZAT</span><h3>{chosen?.service}</h3><p>{chosen?.description}</p><ol><li><span>01</span> Înțelegem contextul</li><li><span>02</span> Clarificăm prioritățile</li><li><span>03</span> Pregătim pașii următori</li></ol><div className="survey-saving-note"><ShieldCheck size={17}/><span>Progres salvat automat.<br/>Link privat pentru reluare.</span></div><Turnstile action="survey-start" onToken={setToken} resetKey={reset}/><button className="survey-button primary" onClick={()=>void start()} disabled={busy||!token||!ready}>{busy?'Pregătim interviul…':'Începe brief-ul'}<ArrowRight size={18}/></button>{error&&<p role="alert" className="survey-error">{error}</p>}<small>Poți alege „nu sunt sigur”. Clarificăm împreună ce lipsește.</small></aside></div></section><ContactLinks/></main></SurveyShell>;
}
